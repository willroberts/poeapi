package poeapi

const (
	// In order to avoid downloading every stash ever published, skip ahead to
	// the latest stash ID as seen by poe.ninja.
	nextChangeURL = "https://poe.ninja/api/Data/GetStats"
)
